# github_repo_using_bolt_ai_builder
Github repository that contains a bash script, when executed the bash script confirms the health of a virtual machine by looking at the parameters such as CPU, Memory, Disk Space etc. The bash script should also support a command line argument "explain", when passed, "explain" provides the detailed summary of health status.
